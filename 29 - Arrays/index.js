const alunos = ['Luiz', 'Maria', 'João', 'Pedro', 'Roberto'];

//alunos.push('Carlos') // Adiciona no fim mais um elemento;
//alunos.unshift('Marcos'); // Adiciona no índice 0;
//alunos.pop(); //Remove o último elemento;
//alunos.shift(); //Remove o primeiro elemento;
//const removidos = alunos.shift(); // Salvando os elementos removidos em variável;
//console.log(alunos instanceof Array); // Pra saber se é um Array
//console.log(alunos.slice(0,3)) // "Fatiar o Array" trazendo do índice 0 ao 2.
//console.log(removidos);
//console.log(alunos);